// generateWhatIfOptions.ts

export function generateWhatIfOptions(scenario: string): string[] {
  // Placeholder logic for generating what-if options
  return [
    `What if we change material for ${scenario}?`,
    `What if we alter the process for ${scenario}?`,
    `What if we increase production volume for ${scenario}?`
  ];
}
